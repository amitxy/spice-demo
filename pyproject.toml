[project]
name = "spice-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "litellm>=1.86.2",
    "pydantic-settings>=2.14.1",
    # Training stack (SFT QLoRA on the A5000 host)
    "unsloth>=2025.1.1",
    "trl>=0.22.2",
    "peft>=0.13.0",
    "torch>=2.4.0",
    "datasets>=2.20.0",
    "wandb>=0.17.0",
    "bitsandbytes>=0.43.0",
]

[tool.uv.sources]
torch = { index = "pytorch-cu124" }

[[tool.uv.index]]
name = "pytorch-cu124"
url = "https://download.pytorch.org/whl/cu124"
explicit = true
